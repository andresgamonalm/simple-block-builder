{
  "name": "mi-publicidad",
  "version": "1.0.0",
  "private": true,
  "description": "Mi Publicidad — publicidad pagada en Google Display, Facebook Ads y Google Search, en Cloudflare Pages + D1",
  "scripts": {
    "dev": "wrangler pages dev .",
    "d1:init": "wrangler d1 execute simple-block-builder --remote --file=./schema/0001_initial.sql",
    "d1:init-local": "wrangler d1 execute simple-block-builder --local --file=./schema/0001_initial.sql"
  },
  "devDependencies": {
    "wrangler": "^3"
  }
}
